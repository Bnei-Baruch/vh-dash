# Local development server configuration.
# Copy to .env.development (gitignored) for `npm start`. No secrets belong here.
PORT=3001
BROWSER=none
HTTPS=false

PUBLIC_URL=/dash

REACT_APP_COMMIT_SHA=dev
